- name: Generate SVG
  run: |
    mkdir -p streak
    streak_count=10  # Replace with actual streak fetching logic
    cat <<EOF > streak/streak.svg
<svg xmlns="http://www.w3.org/2000/svg" width="300" height="50">
  <rect width="300" height="50" fill="red"/>
  <text x="50%" y="50%" dominant-baseline="middle" text-anchor="middle" font-size="20" fill="white">🔥 ${streak_count}-day Streak</text>
</svg>
EOF
